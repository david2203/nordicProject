//environmental variables that can be changed if the server is deployed so that not every line of code has to be changed manually (localhost->heroku)

const server = "http://localhost:1337"

export default server